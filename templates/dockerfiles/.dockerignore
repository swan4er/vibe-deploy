# Зависимости
node_modules
venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache

# Git
.git
.gitignore

# Env-файлы (секреты не должны попадать в образ!)
.env
.env.*
!.env.example

# IDE
.vscode
.idea
*.swp
*.swo

# Документация и тесты
LICENSE
tests
__tests__
coverage
.nyc_output

# Docker
Dockerfile
docker-compose*.yml
compose*.yml
.dockerignore

# Деплой-артефакты
deploy-state.md
DEPLOY.md

# Агент: НЕ исключай директории сборки (dist, build, .next, out),
# если проект без билд-шага в Docker (Вариант B — JavaScript без сборки).
# Раскомментируй строки ниже ТОЛЬКО если Dockerfile содержит билд-шаг:
# dist
# build
# .next
# out
